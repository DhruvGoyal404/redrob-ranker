# Full dependencies: the ranking pipeline + eval harness + the Hugging Face Spaces demo.
# This is what the live demo (Hugging Face Spaces, which supports PyTorch) installs.
# The minimal CPU-only RANKING subset (no torch) used by the Docker image is in
# requirements-rank.txt; test-only deps are in requirements-dev.txt.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=2.2
streamlit>=1.30
pandas>=2.0
numpy>=1.24,<3
rank-bm25>=0.2.2
scikit-learn>=1.3
PyYAML>=6.0
