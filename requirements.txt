# Dependencies for the ranking pipeline, the eval harness, and the Streamlit demo.
#
# Intentionally LIGHT (no torch / sentence-transformers) and LOOSELY pinned so the
# hosted demo resolves cleanly on free 1 GB hosting (Streamlit Community Cloud).
# rank.py and the default demo run on BM25 + the signal scorer and never import
# torch. The offline embedding precompute needs the heavy deps -> install those
# from requirements-precompute.txt. Exact, reproducible pins for the ranking step
# live in requirements-rank.txt (used by the Docker image).

streamlit>=1.30
pandas>=2.0
numpy>=1.24,<3
rank-bm25>=0.2.2
scikit-learn>=1.3
PyYAML>=6.0
