__pycache__/
*.pyc
.venv/
venv/
.DS_Store
.ipynb_checkpoints/

# Large data - not committed; documented in README how to obtain
candidates.jsonl
candidates.jsonl.gz

# Heavy precomputed artifacts (rebuildable via precompute.py, moved via Drive).
# embeddings.npy is ~153 MB - over GitHub's 100 MB file limit - never commit it.
artifacts/*
!artifacts/.gitkeep

# submission.csv = transient rank.py output; .xlsx = portal export. Both rebuildable.
# paradise0211.csv (the final ranked top-100) IS committed as result evidence.
submission.csv
paradise0211.xlsx

artifacts.tgz
*.tgz
images/
ranked_sample.csv